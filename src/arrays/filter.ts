// Implement a function called filter which accepts an array and a callback function.
// The callback function be invoked for each element of the input array.
// If the predicate function returns true, the element will be added to a new array.
// The filter function will return a new array of elements.

export default function filter<T>(
  array: Array<T>,
  callback: (el: T) => boolean,
): Array<T> {
  // do stuff
  return [];
}
