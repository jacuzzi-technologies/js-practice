// Implement a function called map. It will accept an array of elements of the same type and a callback.
// Each element of the array will be passed to the callback, which when invoked will return a new element.
// The function should return a new array of the transformed elements.

// **REMOVE THIS LINE**
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function map<T>(array: Array<T>, callback: (element: T) => T): Array<T> {
  // do stuff
  return [];
}
